// browser-preload.js
// This preload script runs in the context of the BrowserView
// and can expose APIs to the BrowserView's renderer process if needed,
// but it's generally best to keep this minimal for security.
